# Generated by scripts/generate-workflows.js — do not edit by hand.
# Update schedule.config.json, then run: npm run generate-workflows

name: Post DE TikToks

on:
  schedule:
    - cron: "0 13 * * 2"
      timezone: Europe/Helsinki
    - cron: "0 13 * * 3"
      timezone: Europe/Helsinki
    - cron: "0 14 * * 3"
      timezone: Europe/Helsinki
    - cron: "0 13 * * 4"
      timezone: Europe/Helsinki
    - cron: "0 14 * * 4"
      timezone: Europe/Helsinki
    - cron: "0 15 * * 4"
      timezone: Europe/Helsinki
    - cron: "0 16 * * 4"
      timezone: Europe/Helsinki
    - cron: "0 13 * * 5"
      timezone: Europe/Helsinki
    - cron: "0 14 * * 5"
      timezone: Europe/Helsinki
    - cron: "0 15 * * 5"
      timezone: Europe/Helsinki
    - cron: "0 16 * * 5"
      timezone: Europe/Helsinki
    - cron: "0 13 * * 6"
      timezone: Europe/Helsinki
    - cron: "0 14 * * 6"
      timezone: Europe/Helsinki
    - cron: "0 15 * * 6"
      timezone: Europe/Helsinki
    - cron: "0 16 * * 6"
      timezone: Europe/Helsinki
    - cron: "0 13 * * 0"
      timezone: Europe/Helsinki
  workflow_dispatch:

jobs:
  post:
    name: Post DE TikToks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run DE posting script
        run: node postVideos.js de
        env:
          CI: "true"
          GOOGLE_CLIENT_EMAIL: ${{ secrets.GOOGLE_CLIENT_EMAIL }}
          GOOGLE_PRIVATE_KEY: ${{ secrets.GOOGLE_PRIVATE_KEY }}
          SHEET_ID_DE: ${{ secrets.SHEET_ID_DE }}
          BLOTATO_API_KEY: ${{ secrets.BLOTATO_API_KEY }}
          BLOTATO_ACCOUNT_ID_DE: ${{ secrets.BLOTATO_ACCOUNT_ID_DE }}
